export type DirectCompanyDefinition = {
  id: string;
  name: string;
  careersUrl: string;
};

// Add companies here only after their matching parser is available.
export const directCompanyCatalog: readonly DirectCompanyDefinition[] = [];
